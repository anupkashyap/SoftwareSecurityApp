<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.anupkashyap.softwaresecurityapp">
    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SoftwareSecurityApp">
        <activity
            android:name=".ContentProviderActivity"
            android:exported="false" />
        <activity
            android:name=".BroadcastReceiverActivity"
            android:exported="false" />
        <activity
            android:name=".ServiceActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver android:name=".BroadcastReceiverActivity"
            android:exported="false">
            <intent-filter>
                <action android:name="com.anupkashyap.CUSTOM_INTENT" />
            </intent-filter>
        </receiver>
        <service android:name=".TimeService" />
    </application>

</manifest>